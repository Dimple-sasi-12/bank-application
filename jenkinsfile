pipeline {
    agent any
    stages {
        stage ("git scm") {
            steps {
                git branch: 'branch1', url: 'https://github.com/Dimple-sasi-12/bank-application.git' 
            }
        }
        stage ("compile") {
            steps {
                sh 'mvn compile'
            }
        }
        stage ("delete networks, images and container") {
            steps {
                sh 'docker stop $(docker ps -q) || true'
                sh 'docker rm $(docker ps -aq) || true'
                sh 'docker rmi $(docker images -q) || true'
                sh 'docker network rm $(docker network ls -q | grep -v -E "bridge|host|none")'
            }
        }
        stage ("create a docker network") {
            steps {
                sh 'docker network create bank-network'
            }
        }
        stage ("run sql database") {
            steps {
                sh 'docker run -d --name mysql --network bank-network -e MYSQL_ROOT_PASSWORD=testZ@123 -e MYSQL_DATABASE=bankkappdb -p 3306:3306 mysql'
            }
        }
        stage ("create docker image and container") {
            steps {
                sh 'docker build -t bank-img .'
                sh 'docker run -itd --name bank-con --network bank-network -p 81:8081 bank-img'
            }
        }
    }
}